events {}
http {
    server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name wallet._;

        location / {
            proxy_pass http://react-wallet:3000;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header HOST $http_host;
        }
    }
}